-- Create Employee Manager Table
CREATE TABLE employees (
  id varchar(50) NOT NULL PRIMARY KEY,
  first varchar(50) NOT NULL,
  last varchar(50) NOT NULL,
  address varchar(100),
  phone varchar(20)
);
